import React, { Component } from "react";
import { Link } from "react-router-dom";
import { connect } from "react-redux";

class RealtorInfo extends Component {
  render() {
    return <h4>Put properties here</h4>;
  }
}

export default connect()(RealtorInfo);
